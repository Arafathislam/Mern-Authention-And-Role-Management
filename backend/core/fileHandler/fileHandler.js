const fs = require("fs");

module.exports = {
  /**
   * @function create --
   * @param String --file content
   * @param String --file name
   * @return {object} --file itself
   * @usercase {await fs.createReadStream(create(fileContent, fileName))}
   */
  create(fileContent, fileName) {
    let file;
    return file;
  },
};
